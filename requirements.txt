# --- Core (required for make_demo.py) ---
numpy>=1.24
scipy>=1.11
opencv-python>=4.8
torch>=2.0
torchvision>=0.15
ultralytics>=8.4.60      # YOLO detection (yolo26s; falls back to yolo11s)
pyyaml>=6.0
imageio>=2.30            # demo GIF export

# --- Cross-camera ReID (optional: enables `--reid`) ---
# torchreid pulls gdown + tensorboard transitively.
torchreid>=1.4
gdown>=4.0
tensorboard>=2.10

# --- Frozen / Non-goal stack (NOT needed for the demo; see README "Not in scope") ---
# Install only if you revive the MLOps / API extensions.
# mlflow>=2.8
# prometheus-client>=0.18
# dvc>=3.0
# fastapi>=0.104
# uvicorn>=0.24

# --- Dev / test ---
pytest>=7.4
